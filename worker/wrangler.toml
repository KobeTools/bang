name = "bang"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# Deploy this worker directly on workers.dev or bind a custom route later.

[vars]
# Frontend origin for requests that should fall through to the UI.
ORIGIN_URL = "https://bang-web.pages.dev"
